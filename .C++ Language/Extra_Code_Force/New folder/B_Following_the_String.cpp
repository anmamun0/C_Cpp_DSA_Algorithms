#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    cin >> T;
    while(T--)
    {
        int N;
        cin >> N;
        int mat[N];
        for (int i = 0; i < N; i++)
        {
            cin >> mat[i];
        }

        string s;
        for (int i = 0; i < N;i++)
        {
            if(mat[i] == 0)
            {

            }

        }
    }

    return 0;
}